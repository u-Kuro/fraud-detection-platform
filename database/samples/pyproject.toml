[project]
name = "seed"
version = "1.0.0"
dependencies = [
    "sqlalchemy==2.0.52",
    # sqlalchemy dependendencies
    "psycopg2-binary==2.9.12",
]

[dependency-groups]
# helpers
lint = [
    "ruff==0.16.5",
]
type_check = [
    "mypy==2.3.1",
]

[tool.uv]
required-version = "0.12.6"
default-groups = [
    "lint",
    "type_check",
]